"""Shared foundations: SE(3) transforms, config, intrinsics, video, HUD primitives.

The transform convention used across the whole project is documented once,
at the top of `core/transforms.py`. Read that first.
"""
