"""Patient reference frame and target guidance (Phase 4).

`relative.py` computes pose relative to the reference body -- the step that
makes the camera movable. `guidance.py` turns that into the numbers a
navigation display shows. `digitize.py` records points touched with the tip.
"""
