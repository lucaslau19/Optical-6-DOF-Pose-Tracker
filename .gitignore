__pycache__/
*.py[cod]
.venv/
venv/
env/
.vscode/
.idea/
.DS_Store

# Generated artefacts: printable targets, captured calibration frames, and
# the fitted intrinsics. All reproducible from the CLI, and the intrinsics in
# particular are specific to one physical camera -- committing someone else's
# would be worse than having none.
output/
